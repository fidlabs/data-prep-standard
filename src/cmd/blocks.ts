/**
 * @param {string} carPath
 * @param {object} [opts]
 * @param {boolean} [opts.verify]
 */
export default function blocksList(carPath: string, opts: object) {
  console.log("block list NYI", carPath, opts);
}
