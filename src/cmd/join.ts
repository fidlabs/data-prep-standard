/**
 * @param {string} carPath
 * @param {object} [opts]
 * @param {boolean} [opts.verify]
 */
export default function join(carPaths: string[], opts: object) {
  console.log("join NYI", carPaths, opts);
}
