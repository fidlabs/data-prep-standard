/**
 * @param {string} carPath
 * @param {object} [opts]
 * @param {boolean} [opts.verify]
 */
export default function rootsList(carPath: string, opts: object) {
  console.log("rootsList NYI", carPath, opts);
}
