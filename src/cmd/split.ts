/**
 * @param {string} carPath
 * @param {object} [opts]
 * @param {boolean} [opts.verify]
 */
export default function split(carPaths: string, opts: object) {
  console.log("split NYI", carPaths, opts);
}
