/**
 * @param {string} carPath
 * @param {object} [opts]
 * @param {boolean} [opts.verify]
 */
export default function unpack(files: string[], opts: object): void {
  console.log("unpack NYI", files, opts);
}
